---
"@ai-hero/sandcastle": patch
---

Fix macOS Docker image builds for the repo-local Sandcastle config and document that generated configs must build the sandbox image from the target project directory before running.
